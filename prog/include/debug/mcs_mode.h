//============================================================================================
/**
 * @file  msc_mode.h
 * @brief MCS利用モード関連
 * @date  2010.03.20
 */
//============================================================================================
#pragma once

#ifdef  PM_DEBUG

//----------------------------------------------------------------
///  MCS利用モードの定義
//----------------------------------------------------------------
typedef enum {
  MCS_USEMODE_NONE = 0,     ///<MCS常時接続を行っていない
  MCS_USEMODE_RESIDENT,     ///<MCS常時接続（ヒープ解析など）
  MCS_USEMODE_SCRDEBUGGER,  ///<スクリプトデバッガ使用

  MCS_USEMODE_MAX,
}MCS_USEMODE;

//----------------------------------------------------------------
/// 利用モードの取得
//----------------------------------------------------------------
extern MCS_USEMODE MCS_USEMODE_Get(void);

//----------------------------------------------------------------
/// 利用モードの設定
//----------------------------------------------------------------
extern void MCS_USEMODE_Set( MCS_USEMODE mode );
#endif

